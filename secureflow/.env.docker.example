# Copy this to secureflow/.env.docker and fill in real values.
# VITE_API_URL is overridden by docker-compose.yml itself (points at the
# backend service by its compose DNS name) — no need to set it here.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_GOOGLE_CLIENT_ID=
